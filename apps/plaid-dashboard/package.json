{
  "name": "plaid-dashboard",
  "private": true,
  "workspaces": ["dashboard", "engine", "db"],
  "scripts": {
    "dev": "turbo run dev",
    "build": "turbo run build",
    "db:migrate": "turbo run migrate --filter=db",
    "db:generate": "turbo run generate --filter=db",
    "db:studio": "turbo run studio --filter=db"
  },
  "devDependencies": {
    "turbo": "^2.3.0",
    "typescript": "^5.7.0"
  },
  "packageManager": "bun@1.1.0"
}
